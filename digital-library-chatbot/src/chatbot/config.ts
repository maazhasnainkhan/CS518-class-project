import { createChatBotMessage } from "react-chatbot-kit";

const botconfig = {
  initialMessages: [
    createChatBotMessage(`Ask me anything about the abstract!`),
  ],
};

export default botconfig;
